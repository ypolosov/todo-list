# Пример .env.example — значения не коммитить в git
# Скопировать в .env и заполнить

# GitHub API (для state-артефакта через GitHub Issues, опционально)
# GITHUB_TOKEN=

# Прочие секреты целевой системы добавляются по ходу фаз
